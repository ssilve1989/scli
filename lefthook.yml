pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{rs}"
      run: cargo fmt --check
    clippy:
      glob: "*.{rs}"
      run: cargo clippy -- -D warnings
    test:
      run: cargo test

commit-msg:
  commands:
    commitlint:
      run: bunx --yes commitlint --edit {1}
